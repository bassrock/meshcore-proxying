# Serial device path (adjust for your system)
SERIAL_PORT=/dev/ttyACM0
SERIAL_BAUD=115200

# Your location (3-letter IATA airport code)
IATA=SEA

# LetsMesh MQTT broker
MQTT_ENABLED=true
MQTT_SERVER=mqtt-us-v1.letsmesh.net
MQTT_PORT=443
MQTT_TRANSPORT=websockets
MQTT_USE_TLS=true

# Server ports (usually no need to change)
# HTTP_PORT=8080
# WS_PORT=3000
# TCP_PORT=5000

# Enable debug logging
# DEBUG=1
